pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                checkout scm
            }
        }
        // stage('Installin node') {
        //     steps {
        //         node install
        //     }
        // }
        // stage('Deploy') {
        //     steps {
        //         //
        //     }
        // }
    }
}